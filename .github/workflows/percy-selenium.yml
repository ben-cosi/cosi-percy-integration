name: Percy Selenium Snapshots

on:
  workflow_dispatch:
  push:
    branches: [ main, staging ]

jobs:
  linux-chrome-firefox:
    runs-on: ubuntu-latest
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci || npm i
      - name: Run Chrome
        run: npm run test:chrome
      - name: Run Firefox
        run: npm run test:firefox

  macos-safari:
    runs-on: macos-latest
    if: always()  # run even if linux job fails
    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci || npm i
      - name: Enable SafariDriver
        run: sudo safaridriver --enable
      - name: Run Safari
        run: npm run test:safari

  # Edge is best run locally or on a Windows runner with Edge installed.
  # Uncomment if you add a Windows job with Edge available.
  # windows-edge:
  #   runs-on: windows-latest
  #   env:
  #     PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #     - name: Install dependencies
  #       run: npm ci || npm i
  #     - name: Run Edge
  #       run: npm run test:edge
